name: HospitalResourceEnv
description: AI environment for hospital resource allocation

state:
  patients_waiting: int
  critical_patients: int
  available_beds: int
  icu_beds: int
  doctors_available: int
  time: int

actions:
  - ALLOCATE_BED
  - MOVE_TO_ICU
  - WAIT

reward: maximize_patient_care